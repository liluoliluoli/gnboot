// Code generated by gorm.io/gen. DO NOT EDIT.
// Code generated by gorm.io/gen. DO NOT EDIT.
// Code generated by gorm.io/gen. DO NOT EDIT.

package query

import (
	"context"
	"strings"

	"gorm.io/gorm"
	"gorm.io/gorm/clause"
	"gorm.io/gorm/schema"

	"gorm.io/gen"
	"gorm.io/gen/field"
	"gorm.io/gen/helper"

	"gorm.io/plugin/dbresolver"

	"gnboot/internal/data/model"
)

func newActor(db *gorm.DB, opts ...gen.DOOption) actor {
	_actor := actor{}

	_actor.actorDo.UseDB(db, opts...)
	_actor.actorDo.UseModel(&model.Actor{})

	tableName := _actor.actorDo.TableName()
	_actor.ALL = field.NewAsterisk(tableName)
	_actor.ID = field.NewUint64(tableName, "id")
	_actor.Name = field.NewString(tableName, "name")
	_actor.OriginalAme = field.NewString(tableName, "original_ame")
	_actor.Adult = field.NewBool(tableName, "adult")
	_actor.Gender = field.NewBool(tableName, "gender")
	_actor.Profile = field.NewString(tableName, "profile")

	_actor.fillFieldMap()

	return _actor
}

// actor 演职人员
type actor struct {
	actorDo actorDo

	ALL         field.Asterisk
	ID          field.Uint64 // 主键
	Name        field.String // 名字
	OriginalAme field.String // 原名
	Adult       field.Bool   // 是否成年
	Gender      field.Bool   // 性别
	Profile     field.String // 演员的照片或头像的URL

	fieldMap map[string]field.Expr
}

func (a actor) Table(newTableName string) *actor {
	a.actorDo.UseTable(newTableName)
	return a.updateTableName(newTableName)
}

func (a actor) As(alias string) *actor {
	a.actorDo.DO = *(a.actorDo.As(alias).(*gen.DO))
	return a.updateTableName(alias)
}

func (a *actor) updateTableName(table string) *actor {
	a.ALL = field.NewAsterisk(table)
	a.ID = field.NewUint64(table, "id")
	a.Name = field.NewString(table, "name")
	a.OriginalAme = field.NewString(table, "original_ame")
	a.Adult = field.NewBool(table, "adult")
	a.Gender = field.NewBool(table, "gender")
	a.Profile = field.NewString(table, "profile")

	a.fillFieldMap()

	return a
}

func (a *actor) WithContext(ctx context.Context) *actorDo { return a.actorDo.WithContext(ctx) }

func (a actor) TableName() string { return a.actorDo.TableName() }

func (a actor) Alias() string { return a.actorDo.Alias() }

func (a actor) Columns(cols ...field.Expr) gen.Columns { return a.actorDo.Columns(cols...) }

func (a *actor) GetFieldByName(fieldName string) (field.OrderExpr, bool) {
	_f, ok := a.fieldMap[fieldName]
	if !ok || _f == nil {
		return nil, false
	}
	_oe, ok := _f.(field.OrderExpr)
	return _oe, ok
}

func (a *actor) fillFieldMap() {
	a.fieldMap = make(map[string]field.Expr, 6)
	a.fieldMap["id"] = a.ID
	a.fieldMap["name"] = a.Name
	a.fieldMap["original_ame"] = a.OriginalAme
	a.fieldMap["adult"] = a.Adult
	a.fieldMap["gender"] = a.Gender
	a.fieldMap["profile"] = a.Profile
}

func (a actor) clone(db *gorm.DB) actor {
	a.actorDo.ReplaceConnPool(db.Statement.ConnPool)
	return a
}

func (a actor) replaceDB(db *gorm.DB) actor {
	a.actorDo.ReplaceDB(db)
	return a
}

type actorDo struct{ gen.DO }

// SELECT * FROM `@@table` WHERE `id` = @id LIMIT 1
func (a actorDo) GetByID(id uint64) (result model.Actor) {
	var params []interface{}

	var generateSQL strings.Builder
	params = append(params, id)
	generateSQL.WriteString("SELECT * FROM `actor` WHERE `id` = ? LIMIT 1 ")

	var executeSQL *gorm.DB
	executeSQL = a.UnderlyingDB().Raw(generateSQL.String(), params...).Take(&result) // ignore_security_alert
	_ = executeSQL

	return
}

// SELECT * FROM `@@table`
// {{where}}
//
//	{{if val != ""}}
//	  {{if strings.HasPrefix(val, "%") && strings.HasSuffix(val, "%")}}
//	    @@col LIKE CONCAT('%', TRIM(BOTH '%' FROM @val), '%')
//	  {{else if strings.HasPrefix(val, "%")}}
//	    @@col LIKE CONCAT('%', TRIM(BOTH '%' FROM @val))
//	  {{else if strings.HasSuffix(val, "%")}}
//	    @@col LIKE CONCAT(TRIM(BOTH '%' FROM @val), '%')
//	  {{else}}
//	    @@col = @val
//	  {{end}}
//	{{end}}
//
// {{end}}
// LIMIT 1
func (a actorDo) GetByCol(col string, val string) (result model.Actor) {
	var params []interface{}

	var generateSQL strings.Builder
	generateSQL.WriteString("SELECT * FROM `actor` ")
	var whereSQL0 strings.Builder
	if val != "" {
		if strings.HasPrefix(val, "%") && strings.HasSuffix(val, "%") {
			params = append(params, val)
			whereSQL0.WriteString(a.Quote(col) + " LIKE CONCAT('%', TRIM(BOTH '%' FROM ?), '%') ")
		} else if strings.HasPrefix(val, "%") {
			params = append(params, val)
			whereSQL0.WriteString(a.Quote(col) + " LIKE CONCAT('%', TRIM(BOTH '%' FROM ?)) ")
		} else if strings.HasSuffix(val, "%") {
			params = append(params, val)
			whereSQL0.WriteString(a.Quote(col) + " LIKE CONCAT(TRIM(BOTH '%' FROM ?), '%') ")
		} else {
			params = append(params, val)
			whereSQL0.WriteString(a.Quote(col) + " = ? ")
		}
	}
	helper.JoinWhereBuilder(&generateSQL, whereSQL0)
	generateSQL.WriteString("LIMIT 1 ")

	var executeSQL *gorm.DB
	executeSQL = a.UnderlyingDB().Raw(generateSQL.String(), params...).Take(&result) // ignore_security_alert
	_ = executeSQL

	return
}

// SELECT * FROM `@@table`
// {{if len(cols) == len(vals)}}
// {{where}}
//
//	  {{for i, col := range cols}}
//	    {{for j, val := range vals}}
//	      {{if i == j}}
//	        {{if val != ""}}
//	          {{if strings.HasPrefix(val, "%") && strings.HasSuffix(val, "%")}}
//	            @@col LIKE CONCAT('%', TRIM(BOTH '%' FROM @val), '%') AND
//	          {{else if strings.HasPrefix(val, "%")}}
//	            @@col LIKE CONCAT('%', TRIM(BOTH '%' FROM @val)) AND
//	          {{else if strings.HasSuffix(val, "%")}}
//	            @@col LIKE CONCAT(TRIM(BOTH '%' FROM @val), '%') AND
//	          {{else}}
//	            @@col = @val AND
//	          {{end}}
//	        {{end}}
//	      {{end}}
//	    {{end}}
//	  {{end}}
//	{{end}}
//
// {{end}}
// LIMIT 1
func (a actorDo) GetByCols(cols []string, vals []string) (result model.Actor) {
	var params []interface{}

	var generateSQL strings.Builder
	generateSQL.WriteString("SELECT * FROM `actor` ")
	if len(cols) == len(vals) {
		var whereSQL0 strings.Builder
		for i, col := range cols {
			for j, val := range vals {
				if i == j {
					if val != "" {
						if strings.HasPrefix(val, "%") && strings.HasSuffix(val, "%") {
							params = append(params, val)
							whereSQL0.WriteString(a.Quote(col) + " LIKE CONCAT('%', TRIM(BOTH '%' FROM ?), '%') AND ")
						} else if strings.HasPrefix(val, "%") {
							params = append(params, val)
							whereSQL0.WriteString(a.Quote(col) + " LIKE CONCAT('%', TRIM(BOTH '%' FROM ?)) AND ")
						} else if strings.HasSuffix(val, "%") {
							params = append(params, val)
							whereSQL0.WriteString(a.Quote(col) + " LIKE CONCAT(TRIM(BOTH '%' FROM ?), '%') AND ")
						} else {
							params = append(params, val)
							whereSQL0.WriteString(a.Quote(col) + " = ? AND ")
						}
					}
				}
			}
		}
		helper.JoinWhereBuilder(&generateSQL, whereSQL0)
	}
	generateSQL.WriteString("LIMIT 1 ")

	var executeSQL *gorm.DB
	executeSQL = a.UnderlyingDB().Raw(generateSQL.String(), params...).Take(&result) // ignore_security_alert
	_ = executeSQL

	return
}

// SELECT * FROM `@@table`
// {{where}}
//
//	{{if val != ""}}
//	  {{if strings.HasPrefix(val, "%") && strings.HasSuffix(val, "%")}}
//	    @@col LIKE CONCAT('%', TRIM(BOTH '%' FROM @val), '%')
//	  {{else if strings.HasPrefix(val, "%")}}
//	    @@col LIKE CONCAT('%', TRIM(BOTH '%' FROM @val))
//	  {{else if strings.HasSuffix(val, "%")}}
//	    @@col LIKE CONCAT(TRIM(BOTH '%' FROM @val), '%')
//	  {{else}}
//	    @@col = @val
//	  {{end}}
//	{{end}}
//
// {{end}}
func (a actorDo) FindByCol(col string, val string) (result []model.Actor) {
	var params []interface{}

	var generateSQL strings.Builder
	generateSQL.WriteString("SELECT * FROM `actor` ")
	var whereSQL0 strings.Builder
	if val != "" {
		if strings.HasPrefix(val, "%") && strings.HasSuffix(val, "%") {
			params = append(params, val)
			whereSQL0.WriteString(a.Quote(col) + " LIKE CONCAT('%', TRIM(BOTH '%' FROM ?), '%') ")
		} else if strings.HasPrefix(val, "%") {
			params = append(params, val)
			whereSQL0.WriteString(a.Quote(col) + " LIKE CONCAT('%', TRIM(BOTH '%' FROM ?)) ")
		} else if strings.HasSuffix(val, "%") {
			params = append(params, val)
			whereSQL0.WriteString(a.Quote(col) + " LIKE CONCAT(TRIM(BOTH '%' FROM ?), '%') ")
		} else {
			params = append(params, val)
			whereSQL0.WriteString(a.Quote(col) + " = ? ")
		}
	}
	helper.JoinWhereBuilder(&generateSQL, whereSQL0)

	var executeSQL *gorm.DB
	executeSQL = a.UnderlyingDB().Raw(generateSQL.String(), params...).Find(&result) // ignore_security_alert
	_ = executeSQL

	return
}

// SELECT * FROM `@@table`
// {{if len(cols) == len(vals)}}
// {{where}}
//
//	  {{for i, col := range cols}}
//	    {{for j, val := range vals}}
//	      {{if i == j}}
//	        {{if val != ""}}
//	          {{if strings.HasPrefix(val, "%") && strings.HasSuffix(val, "%")}}
//	            @@col LIKE CONCAT('%', TRIM(BOTH '%' FROM @val), '%') AND
//	          {{else if strings.HasPrefix(val, "%")}}
//	            @@col LIKE CONCAT('%', TRIM(BOTH '%' FROM @val)) AND
//	          {{else if strings.HasSuffix(val, "%")}}
//	            @@col LIKE CONCAT(TRIM(BOTH '%' FROM @val), '%') AND
//	          {{else}}
//	            @@col = @val AND
//	          {{end}}
//	        {{end}}
//	      {{end}}
//	    {{end}}
//	  {{end}}
//	{{end}}
//
// {{end}}
func (a actorDo) FindByCols(cols []string, vals []string) (result []model.Actor) {
	var params []interface{}

	var generateSQL strings.Builder
	generateSQL.WriteString("SELECT * FROM `actor` ")
	if len(cols) == len(vals) {
		var whereSQL0 strings.Builder
		for i, col := range cols {
			for j, val := range vals {
				if i == j {
					if val != "" {
						if strings.HasPrefix(val, "%") && strings.HasSuffix(val, "%") {
							params = append(params, val)
							whereSQL0.WriteString(a.Quote(col) + " LIKE CONCAT('%', TRIM(BOTH '%' FROM ?), '%') AND ")
						} else if strings.HasPrefix(val, "%") {
							params = append(params, val)
							whereSQL0.WriteString(a.Quote(col) + " LIKE CONCAT('%', TRIM(BOTH '%' FROM ?)) AND ")
						} else if strings.HasSuffix(val, "%") {
							params = append(params, val)
							whereSQL0.WriteString(a.Quote(col) + " LIKE CONCAT(TRIM(BOTH '%' FROM ?), '%') AND ")
						} else {
							params = append(params, val)
							whereSQL0.WriteString(a.Quote(col) + " = ? AND ")
						}
					}
				}
			}
		}
		helper.JoinWhereBuilder(&generateSQL, whereSQL0)
	}

	var executeSQL *gorm.DB
	executeSQL = a.UnderlyingDB().Raw(generateSQL.String(), params...).Find(&result) // ignore_security_alert
	_ = executeSQL

	return
}

func (a actorDo) Debug() *actorDo {
	return a.withDO(a.DO.Debug())
}

func (a actorDo) WithContext(ctx context.Context) *actorDo {
	return a.withDO(a.DO.WithContext(ctx))
}

func (a actorDo) ReadDB() *actorDo {
	return a.Clauses(dbresolver.Read)
}

func (a actorDo) WriteDB() *actorDo {
	return a.Clauses(dbresolver.Write)
}

func (a actorDo) Session(config *gorm.Session) *actorDo {
	return a.withDO(a.DO.Session(config))
}

func (a actorDo) Clauses(conds ...clause.Expression) *actorDo {
	return a.withDO(a.DO.Clauses(conds...))
}

func (a actorDo) Returning(value interface{}, columns ...string) *actorDo {
	return a.withDO(a.DO.Returning(value, columns...))
}

func (a actorDo) Not(conds ...gen.Condition) *actorDo {
	return a.withDO(a.DO.Not(conds...))
}

func (a actorDo) Or(conds ...gen.Condition) *actorDo {
	return a.withDO(a.DO.Or(conds...))
}

func (a actorDo) Select(conds ...field.Expr) *actorDo {
	return a.withDO(a.DO.Select(conds...))
}

func (a actorDo) Where(conds ...gen.Condition) *actorDo {
	return a.withDO(a.DO.Where(conds...))
}

func (a actorDo) Order(conds ...field.Expr) *actorDo {
	return a.withDO(a.DO.Order(conds...))
}

func (a actorDo) Distinct(cols ...field.Expr) *actorDo {
	return a.withDO(a.DO.Distinct(cols...))
}

func (a actorDo) Omit(cols ...field.Expr) *actorDo {
	return a.withDO(a.DO.Omit(cols...))
}

func (a actorDo) Join(table schema.Tabler, on ...field.Expr) *actorDo {
	return a.withDO(a.DO.Join(table, on...))
}

func (a actorDo) LeftJoin(table schema.Tabler, on ...field.Expr) *actorDo {
	return a.withDO(a.DO.LeftJoin(table, on...))
}

func (a actorDo) RightJoin(table schema.Tabler, on ...field.Expr) *actorDo {
	return a.withDO(a.DO.RightJoin(table, on...))
}

func (a actorDo) Group(cols ...field.Expr) *actorDo {
	return a.withDO(a.DO.Group(cols...))
}

func (a actorDo) Having(conds ...gen.Condition) *actorDo {
	return a.withDO(a.DO.Having(conds...))
}

func (a actorDo) Limit(limit int) *actorDo {
	return a.withDO(a.DO.Limit(limit))
}

func (a actorDo) Offset(offset int) *actorDo {
	return a.withDO(a.DO.Offset(offset))
}

func (a actorDo) Scopes(funcs ...func(gen.Dao) gen.Dao) *actorDo {
	return a.withDO(a.DO.Scopes(funcs...))
}

func (a actorDo) Unscoped() *actorDo {
	return a.withDO(a.DO.Unscoped())
}

func (a actorDo) Create(values ...*model.Actor) error {
	if len(values) == 0 {
		return nil
	}
	return a.DO.Create(values)
}

func (a actorDo) CreateInBatches(values []*model.Actor, batchSize int) error {
	return a.DO.CreateInBatches(values, batchSize)
}

// Save : !!! underlying implementation is different with GORM
// The method is equivalent to executing the statement: db.Clauses(clause.OnConflict{UpdateAll: true}).Create(values)
func (a actorDo) Save(values ...*model.Actor) error {
	if len(values) == 0 {
		return nil
	}
	return a.DO.Save(values)
}

func (a actorDo) First() (*model.Actor, error) {
	if result, err := a.DO.First(); err != nil {
		return nil, err
	} else {
		return result.(*model.Actor), nil
	}
}

func (a actorDo) Take() (*model.Actor, error) {
	if result, err := a.DO.Take(); err != nil {
		return nil, err
	} else {
		return result.(*model.Actor), nil
	}
}

func (a actorDo) Last() (*model.Actor, error) {
	if result, err := a.DO.Last(); err != nil {
		return nil, err
	} else {
		return result.(*model.Actor), nil
	}
}

func (a actorDo) Find() ([]*model.Actor, error) {
	result, err := a.DO.Find()
	return result.([]*model.Actor), err
}

func (a actorDo) FindInBatch(batchSize int, fc func(tx gen.Dao, batch int) error) (results []*model.Actor, err error) {
	buf := make([]*model.Actor, 0, batchSize)
	err = a.DO.FindInBatches(&buf, batchSize, func(tx gen.Dao, batch int) error {
		defer func() { results = append(results, buf...) }()
		return fc(tx, batch)
	})
	return results, err
}

func (a actorDo) FindInBatches(result *[]*model.Actor, batchSize int, fc func(tx gen.Dao, batch int) error) error {
	return a.DO.FindInBatches(result, batchSize, fc)
}

func (a actorDo) Attrs(attrs ...field.AssignExpr) *actorDo {
	return a.withDO(a.DO.Attrs(attrs...))
}

func (a actorDo) Assign(attrs ...field.AssignExpr) *actorDo {
	return a.withDO(a.DO.Assign(attrs...))
}

func (a actorDo) Joins(fields ...field.RelationField) *actorDo {
	for _, _f := range fields {
		a = *a.withDO(a.DO.Joins(_f))
	}
	return &a
}

func (a actorDo) Preload(fields ...field.RelationField) *actorDo {
	for _, _f := range fields {
		a = *a.withDO(a.DO.Preload(_f))
	}
	return &a
}

func (a actorDo) FirstOrInit() (*model.Actor, error) {
	if result, err := a.DO.FirstOrInit(); err != nil {
		return nil, err
	} else {
		return result.(*model.Actor), nil
	}
}

func (a actorDo) FirstOrCreate() (*model.Actor, error) {
	if result, err := a.DO.FirstOrCreate(); err != nil {
		return nil, err
	} else {
		return result.(*model.Actor), nil
	}
}

func (a actorDo) FindByPage(offset int, limit int) (result []*model.Actor, count int64, err error) {
	result, err = a.Offset(offset).Limit(limit).Find()
	if err != nil {
		return
	}

	if size := len(result); 0 < limit && 0 < size && size < limit {
		count = int64(size + offset)
		return
	}

	count, err = a.Offset(-1).Limit(-1).Count()
	return
}

func (a actorDo) ScanByPage(result interface{}, offset int, limit int) (count int64, err error) {
	count, err = a.Count()
	if err != nil {
		return
	}

	err = a.Offset(offset).Limit(limit).Scan(result)
	return
}

func (a actorDo) Scan(result interface{}) (err error) {
	return a.DO.Scan(result)
}

func (a actorDo) Delete(models ...*model.Actor) (result gen.ResultInfo, err error) {
	return a.DO.Delete(models)
}

func (a *actorDo) withDO(do gen.Dao) *actorDo {
	a.DO = *do.(*gen.DO)
	return a
}
